const Orders = () => {
  return (
    <section className="page-card">
      <h2>Orders</h2>
      <p>View and manage all incoming cafeteria orders in one place.</p>
    </section>
  );
};

export default Orders;
